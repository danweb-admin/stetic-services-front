apiVersion: v1
kind: Service
metadata:
  name: demo-agenda-front-svc
  namespace: demo-danweb
  labels:
    app: demo-agenda-front
spec:
  ports:
  - protocol: TCP
    port: 80
  selector:
    app: demo-agenda-front